version: "3.9"

services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: tictactoe_bet
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../db/migrations:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/tictactoe_bet
      REDIS_URL: redis://redis:6379/0
      ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET}
      BOT_TOKEN: ${BOT_TOKEN}
    ports:
      - "8000:8000"

  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile.bot
    restart: unless-stopped
    depends_on:
      - api
      - redis
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      API_BASE_URL: http://api:8000
      TELEBIRR_INSTRUCTIONS: ${TELEBIRR_INSTRUCTIONS}
      NIB_BANK_INSTRUCTIONS: ${NIB_BANK_INSTRUCTIONS}

volumes:
  postgres_data:
